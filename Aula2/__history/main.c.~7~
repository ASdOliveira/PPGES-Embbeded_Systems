#include <main.h>
#include <keyboard.h>

uint8_t lerTec = 0, lerTecAtual = 0; delayCount = 0;
void main()
{
   key_init();
   while(TRUE)
   {
    uint8_t a = key_read();
    if(a != 0xFF)
    {
      output_a(a);
    }
     delay_ms(10);
   }

}
