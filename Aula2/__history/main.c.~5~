#include <main.h>
#include <keyboard.h>


void main()
{
   key_init();
   while(TRUE)
   {
    uint8_t a = key_read();
    if(key_read() != 0xFF)
    {
      output_a(a);
    }
     delay_ms(10);
   }

}
